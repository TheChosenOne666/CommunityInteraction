<?xml version="1.0" encoding="UTF-8"?>
<!-- @author <a href="https://github.com/TheChosenOne666">小楼</a> -->
<!-- @from <a href="https://github.com/TheChosenOne666">TheChosenOne666</a> -->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaolou.community.mapper.PostThumbMapper">

    <resultMap id="BaseResultMap" type="com.xiaolou.community.model.entity.Thumb">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="postId" column="postId" jdbcType="BIGINT"/>
            <result property="userId" column="userId" jdbcType="BIGINT"/>
            <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,postId,
        userId,createTime,updateTime
    </sql>

    <select id="listMyThumbPosts"
            resultType="com.xiaolou.community.model.entity.Post">
        select p.*
        from post p
                 join (select postId from post_thumb where userId = #{userId}) pt
                      on p.id = pt.postId
        where p.isDelete = 0
        order by pt.createTime desc
    </select>
</mapper>
