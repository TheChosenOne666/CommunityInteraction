<?xml version="1.0" encoding="UTF-8"?>
<!-- @author <a href="https://github.com/TheChosenOne666">小楼</a> -->
<!-- @from <a href="https://github.com/TheChosenOne666">TheChosenOne666</a> -->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaolou.community.mapper.PostMapper">

    <resultMap id="BaseResultMap" type="com.xiaolou.community.model.entity.Post">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="coverImg" column="coverImg" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="tags" column="tags" jdbcType="VARCHAR"/>
        <result property="thumbNum" column="thumbNum" jdbcType="BIGINT"/>
        <result property="favourNum" column="favourNum" jdbcType="BIGINT"/>
        <result property="userId" column="userId" jdbcType="BIGINT"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="isDelete" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,title,coverImg,content,tags,
        thumbNum,favourNum,userId,
        createTime,updateTime,isDelete
    </sql>

    <select id="listPostWithDelete" resultType="com.xiaolou.community.model.entity.Post">
        select *
        from post
        where updateTime >= #{minUpdateTime}
    </select>

    <update id="batchUpdateThumbNum">
        UPDATE post
        SET thumbNum = thumbNum + CASE id
        <foreach collection="numMap.entrySet()" item="value" index="key">
            WHEN #{key} THEN #{value}
        </foreach>
        END
        WHERE id IN
        <foreach collection="numMap.keySet()" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
